body{
  background: oldlace;
  font-family: 'Nanum Pen Script', cursive;
}
.container {
  width: 100%;
  height: 100%;
}
a{
  all: unset;
}
header {
  padding: 24px 0;
  text-align: center;
  font-weight: bold;
  border-bottom: solid 1px #ccc;
  font-size: 46px;
  letter-spacing: 5px;
}
main {
  margin: 128px 0;
  display: flex;
  flex-direction: row;
  justify-content: space-evenly;
}

main>div>a {
  cursor: pointer;
  display: flex;
  flex-direction: column;
  gap: 16px;
  align-items: center;
  font-size: 20px;
}
.click_circle{
  width: 230px;
  height: 230px;
  border-radius: 50%;
  align-items: center;
  display: grid;
}
.click_circle:hover{
  background-color: bisque;
  animation: 1s keyRing ease-in-out;
}

@keyframes keyRing {
  0%   {
    transform: scale(1)
  }
  100%  {
    transform: scale(1)
  }
}
